# Autonomous Navigation via Adversarial Imitation Learning

# Target environment
#   - Python: 3.10+
#   - OS: Ubuntu 24.04
#   - ROS 2: Jazzy
#   - Simulator: Gazebo (gz-sim, Harmonic — ships with ROS 2 Jazzy)
#   - GPU: NVIDIA RTX 5080 (Blackwell, sm_120), driver 580+
#
# ROS 2 / Gazebo packages are NOT pip-installable. Install via apt:
#   sudo apt install \
#       ros-jazzy-desktop \
#       ros-jazzy-ros-gz \
#       ros-jazzy-ros-gz-bridge \
#       ros-jazzy-ros-gz-sim \
#       ros-jazzy-nav-msgs \
#       ros-jazzy-sensor-msgs \
#       ros-jazzy-geometry-msgs
#
# PyTorch for RTX 5080 (Blackwell)
#   The default PyPI wheel ships with CUDA 12.4 and will NOT see sm_120.
#   Install the CUDA 12.8 build from PyTorch's index BEFORE pip-installing
#   the rest of this file:
#       pip install torch --index-url https://download.pytorch.org/whl/cu128
#   Then:
#       pip install -r requirements.txt

# Deep learning 
# Listed for completeness; install via the CUDA 12.8 index above first.
torch>=2.7.0

# Numerics/data
numpy>=1.26,<2.3
h5py>=3.10      # expert demonstration storage (HDF5)
scipy>=1.11    # optional: low-pass filtering of teleop input

# RL environment interface
gymnasium>=0.29

# Configuration & utilities
pyyaml>=6.0
tqdm>=4.66

# Logging
tensorboard>=2.16
wandb>=0.16

# Teleop input
# pygame talks to SDL2, which exposes the DualSense as a standard gamepad
# once it's paired over Bluetooth. No vendor-specific driver needed for
# basic stick/button reads.
pygame>=2.5

# Development/testing 
pytest>=8.0
pytest-cov>=4.1
